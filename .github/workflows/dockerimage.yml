name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: echo ${{ github.ref }}
#      run: docker build . --file Dockerfile --tag rtakaishi/vault-token-renewer:$(date +%s)
